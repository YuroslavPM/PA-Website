@using Microsoft.AspNetCore.Identity
@inject UserManager<User> UserManager
@model IEnumerable<PA_Website.Models.UserService>

@{
ViewData["Title"] = "Административен Панел";
var user = await UserManager.GetUserAsync(User);
Layout = "_Layout";

// Get statistics - handle null Model
var reservations = Model ?? new List<PA_Website.Models.UserService>();
var totalReservations = reservations.Count();
var pendingReservations = reservations.Count(r => r.Status == "Pending");
var confirmedReservations = reservations.Count(r => r.Status == "Confirmed");
var completedReservations = reservations.Count(r => r.Status == "Completed");
var cancelledReservations = reservations.Count(r => r.Status == "Cancelled");
var recentReservations = reservations.OrderByDescending(r => r.ReservationDate).Take(4).ToList();
}

@section Styles {
<style>
    .admin-dashboard-container {
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        min-height: calc(100vh - 80px);
        padding: 2rem 0;
    }

    .admin-dashboard-header {
        background: linear-gradient(135deg, #fff 0%, #f8fafc 100%);
        border-radius: 20px;
        box-shadow: 0 15px 30px -10px rgba(124, 58, 237, 0.1);
        padding: 2rem;
        margin-bottom: 1.5rem;
        position: relative;
        overflow: hidden;
    }

    .admin-dashboard-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 6px;
        background: linear-gradient(90deg, #4c1d95, #7c3aed);
    }

    .admin-dashboard-title {
        font-family: 'Playfair Display', serif;
        font-size: 2rem;
        font-weight: 700;
        background: linear-gradient(135deg, #4c1d95, #7c3aed);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
        position: relative;
    }

    .admin-dashboard-title::after {
        content: '';
        position: absolute;
        bottom: -12px;
        left: 0;
        width: 100px;
        height: 4px;
        background: linear-gradient(90deg, #4c1d95, #7c3aed);
        border-radius: 2px;
    }

    .admin-dashboard-subtitle {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .stat-card {
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border-left: 4px solid;
    }

    .stat-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
    }

    .stat-card.total {
        border-left-color: #4c1d95;
    }

    .stat-card.pending {
        border-left-color: #fbbf24;
    }

    .stat-card.confirmed {
        border-left-color: #10b981;
    }

    .stat-card.completed {
        border-left-color: #6b7280;
    }

    .stat-card.cancelled {
        border-left-color: #ef4444;
    }

    .stat-icon {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1rem;
        font-size: 1.5rem;
        color: white;
    }

    .stat-icon.total {
        background: linear-gradient(135deg, #4c1d95, #7c3aed);
    }

    .stat-icon.pending {
        background: linear-gradient(135deg, #fbbf24, #f59e0b);
    }

    .stat-icon.confirmed {
        background: linear-gradient(135deg, #10b981, #059669);
    }

    .stat-icon.completed {
        background: linear-gradient(135deg, #6b7280, #4b5563);
    }

    .stat-icon.cancelled {
        background: linear-gradient(135deg, #ef4444, #dc2626);
    }

    .stat-number {
        font-size: 2rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 0.5rem;
    }

    .stat-label {
        color: #64748b;
        font-size: 0.9rem;
        font-weight: 500;
    }

    .admin-actions-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .admin-action-card {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid rgba(124, 58, 237, 0.1);
    }

    .admin-action-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
        border-color: rgba(124, 58, 237, 0.2);
    }

    .admin-action-icon {
        width: 64px;
        height: 64px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1rem;
        font-size: 2rem;
        color: white;
        background: linear-gradient(135deg, #4c1d95, #7c3aed);
    }

    .admin-action-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: #1e293b;
        margin-bottom: 0.5rem;
    }

    .admin-action-description {
        color: #64748b;
        font-size: 0.9rem;
        margin-bottom: 1.5rem;
    }

    .btn-admin {
        background: linear-gradient(135deg, #4c1d95, #7c3aed);
        color: white;
        border: none;
        border-radius: 12px;
        padding: 10px 20px;
        font-size: 0.9rem;
        font-weight: 600;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 4px 6px -1px rgba(124, 58, 237, 0.2);
    }

    .btn-admin:hover {
        background: linear-gradient(135deg, #7c3aed, #4c1d95);
        transform: translateY(-2px);
        box-shadow: 0 10px 25px -5px rgba(124, 58, 237, 0.4);
        color: white;
    }

    .recent-reservations {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    .recent-reservations-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: #1e293b;
        margin-bottom: 1.5rem;
    }

    .reservation-item {
        display: flex;
        flex-direction: column;
        padding: 1rem;
        border-bottom: 1px solid #e2e8f0;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .reservation-item:hover {
        background: #f8fafc;
    }

    .reservation-item:last-child {
        border-bottom: none;
    }

    .reservation-info {
        flex: 1;
        margin-bottom: 1rem;
    }

    .reservation-user {
        font-weight: 600;
        color: #1e293b;
        margin-bottom: 0.25rem;
    }

    .reservation-service {
        color: #64748b;
        font-size: 0.9rem;
    }

    .reservation-date {
        color: #64748b;
        font-size: 0.875rem;
    }

    .reservation-actions {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .status-badge {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    .status-pending {
        background: linear-gradient(135deg, #fbbf24, #f59e0b);
        color: white;
    }

    .status-confirmed {
        background: linear-gradient(135deg, #10b981, #059669);
        color: white;
    }

    .status-completed {
        background: linear-gradient(135deg, #6b7280, #4b5563);
        color: white;
    }

    .status-cancelled {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
    }

    /* Financial Dashboard Styles */
    .financial-dashboard {
        margin-bottom: 2rem;
    }

    .financial-header {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .financial-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #4c1d95;
    }

    .financial-subtitle {
        color: #64748b;
    }

    .financial-controls {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .financial-select {
        padding: 0.5rem 1rem;
        border-radius: 8px;
        border: 1px solid #e2e8f0;
        background-color: white;
        color: #1e293b;
    }

    .financial-summary {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .total-earnings {
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    .earnings-amount {
        font-size: 2rem;
        font-weight: 700;
        color: #10b981;
        margin-bottom: 0.5rem;
    }

    .earnings-label {
        color: #64748b;
    }

    .chart-container {
        width: 100%;
        height: 300px;
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    /* Responsive Adjustments */
    @@media (min-width: 640px) {
        .reservation-item {
            flex-direction: row;
            justify-content: space-between;
            align-items: center;
        }

        .reservation-info {
            margin-bottom: 0;
        }

        .reservation-actions {
            flex-direction: row;
            align-items: center;
        }

        .financial-header {
            flex-direction: row;
            justify-content: space-between;
            align-items: flex-end;
        }

        .financial-controls {
            flex-direction: row;
            align-items: flex-end;
        }

        .financial-summary {
            flex-direction: row;
            align-items: center;
        }

        .total-earnings {
            flex: 1;
        }

        .chart-container {
            flex: 2;
        }
    }

    @@media (max-width: 768px) {
        .stats-grid {
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        }

        .admin-actions-grid {
            grid-template-columns: 1fr;
        }

        .admin-dashboard-title {
            font-size: 1.75rem;
        }

        .admin-dashboard-subtitle {
            font-size: 1rem;
        }

        .stat-number {
            font-size: 1.75rem;
        }

        .admin-action-title {
            font-size: 1.1rem;
        }
    }

    @@media (max-width: 480px) {
        .stats-grid {
            grid-template-columns: 1fr;
        }

        .stat-card {
            padding: 1.25rem;
        }

        .admin-dashboard-header {
            padding: 1.5rem;
        }

        .admin-dashboard-title {
            font-size: 1.5rem;
        }

        .btn-admin {
            width: 100%;
            text-align: center;
        }

        .reservation-actions a {
            width: 100%;
        }
    }
</style>
}

<div class="admin-dashboard-container">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="admin-dashboard-header">
            <h1 class="admin-dashboard-title">Административен Панел</h1>
            <p class="admin-dashboard-subtitle">Добре дошли, @user.FName @user.LName! Управлявайте вашия сайт от тук.</p>
        </div>

        <!-- Financial Dashboard -->
        <div class="financial-dashboard" id="financial-dashboard">
            <div class="financial-header">
                <div>
                    <h2 class="financial-title">Финансов преглед</h2>
                    <p class="financial-subtitle">Общ приход от завършени резервации за избрания период</p>
                </div>
                <div class="financial-controls">
                    <div>
                        <label for="yearSelect" class="block mb-1 text-sm font-medium text-gray-700">Година:</label>
                        <select id="yearSelect" class="financial-select w-full"></select>
                    </div>
                    <div>
                        <label for="monthSelect" class="block mb-1 text-sm font-medium text-gray-700">Месец:</label>
                        <select id="monthSelect" class="financial-select w-full">
                            <option value="">Всички</option>
                            <option value="1">Януари</option>
                            <option value="2">Февруари</option>
                            <option value="3">Март</option>
                            <option value="4">Април</option>
                            <option value="5">Май</option>
                            <option value="6">Юни</option>
                            <option value="7">Юли</option>
                            <option value="8">Август</option>
                            <option value="9">Септември</option>
                            <option value="10">Октомври</option>
                            <option value="11">Ноември</option>
                            <option value="12">Декември</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="financial-summary">
                <div class="total-earnings">
                    <div class="flex items-center justify-center gap-3 mb-2">
                        <div class="earnings-amount" id="totalEarnings">0.00 €</div>
                        <div id="percentChange" class="text-lg font-semibold"></div>
                    </div>
                    <div class="earnings-label">Общ приход</div>
                </div>
                <div class="chart-container">
                    <canvas id="earningsChart"></canvas>
                </div>
            </div>
        </div>
        <!-- End Financial Dashboard -->

        <!-- Statistics -->
        <div class="stats-grid">
            <div class="stat-card total">
                <div class="stat-icon total">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <div class="stat-number">@totalReservations</div>
                <div class="stat-label">Общо резервации</div>
            </div>

            <div class="stat-card pending">
                <div class="stat-icon pending">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="stat-number">@pendingReservations</div>
                <div class="stat-label">Чакащи</div>
            </div>

            <div class="stat-card confirmed">
                <div class="stat-icon confirmed">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="stat-number">@confirmedReservations</div>
                <div class="stat-label">Потвърдени</div>
            </div>

            <div class="stat-card completed">
                <div class="stat-icon completed">
                    <i class="fas fa-check-double"></i>
                </div>
                <div class="stat-number">@completedReservations</div>
                <div class="stat-label">Завършени</div>
            </div>

            <div class="stat-card cancelled">
                <div class="stat-icon cancelled">
                    <i class="fas fa-times-circle"></i>
                </div>
                <div class="stat-number">@cancelledReservations</div>
                <div class="stat-label">Отменени</div>
            </div>
        </div>

        <!-- Admin Actions -->
        <div class="admin-actions-grid">
            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3 class="admin-action-title">Управление на резервации</h3>
                <p class="admin-action-description">Преглед и управление на всички резервации от потребителите</p>
                <a href="@Url.Action("IndexAdmin", "UserServices")" class="btn-admin">
                    <i class="fas fa-arrow-right mr-2"></i> Управлявай резервации
                </a>
            </div>

            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-magic"></i>
                </div>
                <h3 class="admin-action-title">Управление на услуги</h3>
                <p class="admin-action-description">Добавяне, редактиране и изтриване на услуги</p>
                <a href="@Url.Action("Index", "Services")" class="btn-admin">
                    <i class="fas fa-arrow-right mr-2"></i> Управлявай услуги
                </a>
            </div>

            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-user-cog"></i>
                </div>
                <h3 class="admin-action-title">Управление на потребители</h3>
                <p class="admin-action-description">Преглед и управление на потребителски акаунти</p>
                <a href="@Url.Action("Index", "Users")" class="btn-admin">
                    <i class="fas fa-arrow-right mr-2"></i> Управлявай потребители
                </a>
            </div>

            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-newspaper"></i>
                </div>
                <h3 class="admin-action-title">Управление на статии</h3>
                <p class="admin-action-description">Добавяне, редактиране и изтриване на статии</p>
                <a href="@Url.Action("Index", "Articles")" class="btn-admin">
                    <i class="fas fa-arrow-right mr-2"></i> Управлявай статии
                </a>
            </div>

            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <h3 class="admin-action-title">Изпращане на имейли</h3>
                <p class="admin-action-description">Изпратете имейли на резервации или масови имейли</p>
                <div class="flex flex-col sm:flex-row gap-2">
                    <a href="@Url.Action("SendEmail", "UserServices")" class="btn-admin">
                        <i class="fas fa-envelope mr-2"></i> Резервации
                    </a>
                    <a href="@Url.Action("SendBulkEmail", "UserServices")" class="btn-admin">
                        <i class="fas fa-users mr-2"></i> Масови
                    </a>
                </div>
            </div>

            <div class="admin-action-card">
                <div class="admin-action-icon">
                    <i class="fas fa-gift"></i>
                </div>
                <h3 class="admin-action-title">Управление на промоции</h3>
                <p class="admin-action-description">Създавайте и управлявайте специални оферти за клиентите</p>
                <a href="@Url.Action("Index", "Promotions")" class="btn-admin">
                    <i class="fas fa-arrow-right mr-2"></i> Управлявай промоции
                </a>
            </div>
        </div>

        <!-- Recent Reservations -->
        <div class="recent-reservations">
            <h3 class="recent-reservations-title">Последни резервации</h3>
            @if (recentReservations.Any())
            {
            @foreach (var reservation in recentReservations)
            {
            <div class="reservation-item">
                <div class="reservation-info">
                    <div class="reservation-user">
                        @if (reservation.User != null)
                        {
                        @reservation.User.FName @reservation.User.LName
                        }
                        else
                        {
                        <span class="text-gray-500 italic">Изтрит потребител</span>
                        }
                    </div>
                    <div class="reservation-service">@reservation.Service.NameService</div>
                    <div class="reservation-date">
                        @reservation.ReservationDate.ToString("dd.MM.yyyy")
                        @if(reservation.ReservationTime.HasValue)
                        {
                        <span>@reservation.ReservationTime.Value.ToString(@"hh\:mm")</span>
                        }
                    </div>
                </div>
                <div class="reservation-actions">
                            <span class="status-badge @(reservation.Status == "Pending" ? "status-pending" :
                            reservation.Status == "Confirmed" ? "status-confirmed" :
                            reservation.Status == "Completed" ? "status-completed" : "status-cancelled")">
                                @(reservation.Status == "Pending" ? "Чакаща" :
                                reservation.Status == "Confirmed" ? "Потвърдена" :
                                reservation.Status == "Completed" ? "Завършена" : "Отменена")
                            </span>
                    <a href="@Url.Action("Details", "UserServices", new { id = reservation.Id })" class="btn-admin">
                        <i class="fas fa-eye mr-2"></i> Детайли
                    </a>
                </div>
            </div>
            }
            <div class="text-center mt-6">
                <a href="@Url.Action("IndexAdmin", "UserServices")" class="btn-admin">
                    <i class="fas fa-list mr-2"></i> Всички резервации
                </a>
            </div>
            }
            else
            {
            <div class="text-center py-8">
                <div class="w-20 h-20 bg-gradient-to-br from-purple-100 to-indigo-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-calendar-times text-3xl text-purple-500"></i>
                </div>
                <p class="text-gray-600">Няма резервации за показване</p>
            </div>
            }
        </div>
    </div>
</div>

@section Scripts {
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    // Admin Dashboard - using vanilla JavaScript (no jQuery dependency)
    document.addEventListener('DOMContentLoaded', function() {
        // Helper: get years from 2023 to current
            function populateYearSelect() {
                var yearSelect = document.getElementById('yearSelect');
                if (!yearSelect) return;
                
                var currentYear = new Date().getFullYear();
                for (var y = currentYear; y >= 2023; y--) {
                    var opt = document.createElement('option');
                    opt.value = y;
                    opt.text = y;
                    yearSelect.appendChild(opt);
                }
                yearSelect.value = currentYear;
            }

            // Chart.js instance
            var earningsChart;

            function fetchFinancials() {
                var yearSelect = document.getElementById('yearSelect');
                var monthSelect = document.getElementById('monthSelect');
                if (!yearSelect || !monthSelect) return;

                var year = yearSelect.value;
                var month = monthSelect.value;
                
                // Use fetch API instead of jQuery ajax for better reliability
                var url = '/UserServices/GetFinancials?year=' + encodeURIComponent(year) + '&month=' + encodeURIComponent(month);
                
                fetch(url, {
                    method: 'GET',
                    headers: {
                        'Accept': 'application/json'
                    }
                })
                .then(response => {
                    if (!response.ok) {
                        throw new Error('Network response was not ok');
                    }
                    return response.json();
                })
                .then(function(data) {
                    // Update total earnings
                    var totalEarningsEl = document.getElementById('totalEarnings');
                    if (totalEarningsEl) {
                        totalEarningsEl.textContent = (data.totalEarnings || 0).toFixed(2) + ' €';
                    }

                    // Show percent change
                    var percentChangeEl = document.getElementById('percentChange');
                    if (percentChangeEl) {
                        var percent = data.percentChange || 0;
                        var percentText = '';
                        if (percent > 0) {
                            percentText = '<span style="color:#10b981;">▲ ' + percent.toFixed(1) + '%</span>';
                        } else if (percent < 0) {
                            percentText = '<span style="color:#ef4444;">▼ ' + percent.toFixed(1) + '%</span>';
                        } else {
                            percentText = '<span style="color:#64748b;">0%</span>';
                        }
                        percentChangeEl.innerHTML = percentText;
                    }

                    // Prepare chart data
                    var labels = [];
                    var values = [];
                    var monthlyData = data.monthlyData || [];
                    for (var i = 1; i <= 12; i++) {
                        var found = monthlyData.find(function(m) { return m.Month === i; });
                        labels.push([
                            'Януари','Февруари','Март','Април','Май','Юни','Юли','Август','Септември','Октомври','Ноември','Декември'
                        ][i-1]);
                        values.push(found ? found.Earnings : 0);
                    }
                    
                    var chartCanvas = document.getElementById('earningsChart');
                    if (chartCanvas && typeof Chart !== 'undefined') {
                        try {
                            if (earningsChart) earningsChart.destroy();
                            var ctx = chartCanvas.getContext('2d');
                            earningsChart = new Chart(ctx, {
                                type: 'bar',
                                data: {
                                    labels: labels,
                                    datasets: [{
                                        label: 'Приходи (€)',
                                        data: values,
                                        backgroundColor: 'rgba(76,29,149,0.7)',
                                        borderRadius: 8
                                    }]
                                },
                                options: {
                                    responsive: true,
                                    maintainAspectRatio: false,
                                    plugins: {
                                        legend: { display: false },
                                        title: { display: false }
                                    },
                                    scales: {
                                        y: {
                                            beginAtZero: true,
                                            grid: {
                                                color: 'rgba(226, 232, 240, 0.5)'
                                            }
                                        },
                                        x: {
                                            grid: {
                                                display: false
                                            }
                                        }
                                    }
                                }
                            });
                        } catch (chartError) {
                            console.error('Error creating chart:', chartError);
                            chartCanvas.parentElement.innerHTML = '<p class="text-gray-600 text-center">Графиката не може да бъде заредена</p>';
                        }
                    } else if (chartCanvas && typeof Chart === 'undefined') {
                        chartCanvas.parentElement.innerHTML = '<p class="text-gray-600 text-center">Графиката не може да бъде заредена</p>';
                    }
                })
                .catch(function(error) {
                    console.error('Error fetching financials:', error);
                    var totalEarningsEl = document.getElementById('totalEarnings');
                    if (totalEarningsEl) {
                        totalEarningsEl.textContent = 'Грешка';
                    }
                });
            }

        // Initialize dashboard
        var yearSelectEl = document.getElementById('yearSelect');
        var monthSelectEl = document.getElementById('monthSelect');
        
        if (yearSelectEl && monthSelectEl) {
            populateYearSelect();
            fetchFinancials();
            
            // Add event listeners for year and month select changes
            yearSelectEl.addEventListener('change', fetchFinancials);
            monthSelectEl.addEventListener('change', fetchFinancials);
        }
    });
</script>
}